// This is where we define the client-side routes

// page ('route', callbalckToExecuteWhenThisRouteIsHit);

page('/', homeView.init);
page('/about', aboutView.init);
page('/contact', contactView.init);
page();
